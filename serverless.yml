service: busgv-bot

provider:
  name: aws
  runtime: python3.6
  stage: dev
  region: us-east-1
  environment:
    - TELEGRAM_BOT_TOKEN: ${env:TELEGRAM_BOT_TOKEN}

package:
  exclude:
    - '*'
  include:
    - funcs/**

functions:
  hello:
    handler: funcs/handler.hello
    events:
      - http:
          path: hello
          method: get

plugins:
  - serverless-python-requirements
