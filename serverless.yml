service: busgv-bot

provider:
  name: aws
  runtime: python3.6
  stage: dev
  region: us-east-1

package:
  exclude:
    - '*'
  include:
    - funcs/**

functions:
  hello:
    handler: funcs/handler.hello
    events:
      - http:
          path: hello
          method: get
